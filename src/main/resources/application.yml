eureka:
  instance:
    appGroupName: dashboard
  client:
    enabled: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
    application:
      name: dashboard
server:
    port: 8083